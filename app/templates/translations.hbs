<table border=1>
  <tbody class="existing">
  {{#each controller}}
  <tr class="separator">
  </tr>
  {{partial 'translation'}}
  {{/each}}
  </tbody>

  {{outlet}}

</table>

{{#link-to 'translations.new'}}New translation{{/link-to}}
